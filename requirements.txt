# Developed against the Kaggle Python GPU notebook image. Versions are not pinned because
# that image is updated by Kaggle independently of this repo; install without version
# constraints to match whatever the current image ships.
numpy
pandas
matplotlib
seaborn
scikit-learn
lightgbm
xgboost
catboost
optuna
shap
torch
joblib

# For tabular_foundation_model_benchmark.ipynb only (the main notebook doesn't need these).
# Also unpinned, same reasoning; tabpfn specifically also needs a TABPFN_TOKEN (see that
# notebook's own markdown) since TabPFN >= 2.5 gates local weight downloads behind a license.
tabpfn
autogluon.tabular[tabicl]
